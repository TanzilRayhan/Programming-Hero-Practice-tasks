var age = [5, 10, 15, 7];

// push to add element at last
age.push(20);
console.log(age);

// pop to remove an element from last
var names = ['top', 'rock', 'pot'];
names.pop();
console.log(names);

// popped element
var element = names.pop();
console.log(element);